#include "types_inl.h"

{index} species_index_gen(const char* name);

{device_option}
{scalar_function}
density_from_massfractions_pressure_temperature({species_parameter} massfractions,
                                                {scalar_parameter} pressure,
                                                {scalar_parameter} temperature) {const_option};

{device_option}
{species_function}
scale_gen({scalar_parameter} a, {species_parameter} b_s) {const_option};

{device_option}
{chemical_state_function}
scale_gen({scalar_parameter} a, {chemical_state_parameter} b_s) {const_option};

{device_option}
{species_function}
operator+({species_parameter} a_s, {species_parameter} b_s) {const_option};

{device_option}
{jacobian_function}
operator+({jacobian_parameter} a, {jacobian_parameter} b) {const_option};

{device_option}
{species_function}
operator-({species_parameter} a_s, {species_parameter} b_s_positive) {const_option};

{device_option}
{chemical_state_function}
operator+({chemical_state_parameter} a_s, {chemical_state_parameter} b_s) {const_option};

{device_option}
{chemical_state_function}
operator-({chemical_state_parameter} a_s, {chemical_state_parameter} b_s_positive) {const_option};

{device_option}
{species_function}
operator*({scalar_parameter} s, {species_parameter} a) {const_option};

{device_option}
{chemical_state_function}
operator*({scalar_parameter} s, {chemical_state_parameter} a) {const_option};

{device_option}
{species_function}
operator*({species_parameter} a_s, {species_parameter} b_s) {const_option};

{device_option}
{scalar_function}
norm2({species_parameter} x) {const_option};

{device_option}
{scalar_function}
divide({scalar_parameter} a, {scalar_parameter} b) {const_option};

{device_option}
{species_function}
divide({species_parameter} a, {species_parameter} b) {const_option};

{device_option}
{scalar_function}
sqrt_gen({scalar_parameter} a) {const_option};

{device_option}
{scalar_function}
scale_gen({scalar_parameter} a, {scalar_parameter} b) {const_option};

{species_function} inv_molecular_weights_();

{device_option}
{species_function}
concentrations_from_molefractions_pressure_temperature({species_parameter} molefractions,
                                                       {scalar_parameter} pressure,
                                                       {scalar_parameter} temperature) {const_option};

{device_option}
{scalar_function}
internal_energy_volume_specific({species_parameter} species,
                                {scalar_parameter} temperature) {const_option};

{device_option}
{chemical_state_function}
set_chemical_state({scalar_parameter} energy,
                   {species_parameter} species) {const_option};

{device_option}
{scalar_function}
temperature({chemical_state_parameter} chemical_state) {const_option};

{device_option}
{scalar_function}
temperature({scalar} energy, {chemical_state_parameter} chemical_state) {const_option};

{device_option}
{chemical_state_function}
source({chemical_state_parameter} chemical_state, {scalar} temperature) {const_option};

{device_option}
{chemical_state_function}
source({chemical_state_parameter} chemical_state) {const_option};

{device_option}
{jacobian_function_eigen}
source_jacobian({chemical_state_parameter} chemical_state, {scalar} temperature, {scalar_parameter} scaling_factor=1, {scalar_parameter} diagonal_add=0) {const_option};

{device_option}
{species_function}
get_species({chemical_state_parameter} y) {const_option};

{device_option}
{scalar_function} error_norm({chemical_state_parameter} x,
                             {chemical_state_parameter} x_ref,
                             {scalar} abs_tolerance,
                             {scalar} rel_tolerance) {const_option};

Matrix<{index}, n_variables, 1> get_perm_indices();
Matrix<{index}, n_variables, 1> get_inv_perm_indices();

{device_option}
{index}
get_lu_perm_row_index({index} i);

{device_option}
{index}
get_lu_perm_col_index({index} i);
